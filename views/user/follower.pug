extends layout
include ../mixins/user_item
include ../mixins/page

block user_content
    if (follower.length != 0)
        each f in follower
            +user_item(f.user,current_user)
        +page(page,pages)
    else
        div.EmptyState
            div.EmptyState-inner
                span 空